import React from 'react'

// export const Child = (props) => { //for single props
export const Child = ({name, resources}) => {
  return (
   <>
    <h6>Welcome to {name} Learn with {resources}</h6>
    
   </>
  )
}
